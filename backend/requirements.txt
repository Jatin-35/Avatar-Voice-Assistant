# Runtime dependencies — deliberately minimal so the backend fits Render's
# free tier (512MB RAM) and builds in seconds instead of pulling the ~3GB
# torch/CUDA stack. Azure TTS + Azure OpenAI are called via plain REST
# (aiohttp), and Deepgram STT via raw WebSocket — no provider SDKs needed.

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ── HTTP / WebSocket ───────────────────────────────────────────────────────────
aiohttp>=3.9.0
websockets>=12.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Optional: RAG / knowledge base (currently unused — no index built) ────────
# rag/service.py detects these are missing and disables RAG gracefully.
# To enable RAG later, install these and run backend/scripts/build_index.py:
#   faiss-cpu sentence-transformers langchain langchain-community
#   langchain-huggingface rank-bm25 numpy
# WARNING: pulls in PyTorch (~3GB) — will NOT fit Render's free tier.
